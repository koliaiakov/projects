apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bstl-website.fullname" . }}-test-connection"
  labels:
    {{- include "bstl-website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bstl-website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
