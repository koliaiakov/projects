apiVersion: v1
kind: Service
metadata:
  name: {{ include "bstl-website.fullname" . }}
  labels:
    {{- include "bstl-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bstl-website.selectorLabels" . | nindent 4 }}
